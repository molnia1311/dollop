apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr
  annotations:
    argocd.argoproj.io/hook: Skip
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
  volumeName: radarr
