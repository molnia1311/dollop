apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anime
  annotations:
    k8up.io/backup: false
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Ti
  storageClassName: local-storage
