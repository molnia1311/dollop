apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transcodes
  annotations:
    k8up.io/backup: "false"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-storage
